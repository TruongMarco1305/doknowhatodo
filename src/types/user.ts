export type User = {
  address: string;
  highSchool: string | null;
  id: string;
  username: string;
  email: string;
  name: string;
  roles: string;
  bio: string;
  company: string;
  imageUrl: string;
};
